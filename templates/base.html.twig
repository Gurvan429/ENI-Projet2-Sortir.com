<!DOCTYPE html>
<html lang="fr">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport"
              content="width=device-width, user-scalable=no, initial-scale=1.0">
        <meta http-equiv="X-UA-Compatible" content="ie=edge">

        <title>{% block title %}Sortir.com1{% endblock %}</title>
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-KK94CHFLLe+nY2dmCWGMq91rCGa5gtU4mk92HdvYe+M/SXH301p5ILy+dN9+nJOZ" crossorigin="anonymous">
        <link rel="stylesheet" href="{{ asset('app.css') }}">
        <link rel="icon" href="{{ asset('img/logosortir.com.png') }}">
    </head>
    <body>
        <div class="container">

            <header>
                {% if app.user %}
                <nav class="navbar">

                    <div class="container-fluid">
                        <a class="navbar-brand" href="{{ path('sorties') }}"><img src="{{ asset('img/logosortir.com.png') }}" alt="Sortir.com" width="180" height="75"></a>

                        <div id="navbarNav">
                            <ul>
                                <li class="nav-item">
                                    <a href="{{ path('sorties') }}" title="Accueil">ACCUEIL</a>
                                </li>
                                <li class="nav-item">
                                    <a   href="{{ path('app_ville_index') }}" title="Villes">VILLES</a>
                                </li>
                                {% if is_granted("ROLE_ADMIN") %}
                                <li class="nav-item">
                                    <a   href="#" title="Campus" style="font-style: italic; color: blue;">CAMPUS</a>
                                </li>
                                <li class="nav-item">
                                    <a   href="{{ path('app_register') }}" title="Créer un compte" style="font-style: italic; color: blue;">CREER UN COMPTE</a>
                                </li>

                                {% endif %}
                                <li class="nav-item">
                                    <a   href="{{ path('edit') }}" title="Mon profil">MON PROFIL</a>
                                </li>
                                <li id="navLogout" class="nav-item">
                                    <a   href="{{ path('app_logout') }}" title="Se déconnecter">SE DECONNECTER ({{ app.user.pseudo }})</a>
                                </li>
                             </ul>
                        </div>
                    </div>
                </nav>
                {% endif %}
            </header>

            <main>{% block body %}{% endblock %}</main>

            <footer>
                <p>&copy;Sortir.com | Gurvan, Julien, Mathieu, Clément | {{ "now"|date("Y") }}</p>
            </footer>
        </div>
    </body>
</html>
